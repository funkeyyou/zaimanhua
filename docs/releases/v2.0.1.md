v2.0.1 改善漫畫橫向翻頁的跨集操作。

- 移除章節邊界的圓形箭頭按鈕動畫。
- 最後一頁短距離快滑即可切換下一集，不必額外長拉；慢速拖動依一般翻頁方式判斷。
- 左右閱讀方向皆適用，往上一集仍定位在最後一頁。
- 防止雙指縮放、上下滑動、小幅拖動，以及滑到本集最後一頁的同一個手勢誤切集。

Android 使用者已驗收通過；81 項自動測試通過。

Android 可從 App 內檢查更新，或下載 `ZAI-X-android.apk` 覆蓋安裝。Windows 請先關閉舊版，將 `ZAI-X-windows-x64.zip` 解壓到新資料夾後執行 `ZAI-X.exe`。既有資料、名稱與圖示沿用原設定。
