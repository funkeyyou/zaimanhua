# 2026-09-09 介面整理與再漫畫X 品牌

狀態：UI 整理與下列詳情頁回歸修正已完成驗證，納入 v2.0.0。功能驗證提交 `22171c5`，發版狀態與產物檢查見 [QA-v2.0.0](QA-v2.0.0.md)。

## 後續驗收修正：簡介與更多章節

- 簡介展開時改用完整換行顯示，收起時才保留三行與省略號。先前展開雖設定 `maxLines=null`，卻仍套用 ellipsis，導致長文反而被截短；展開／收起按鈕文字本身對應正確。
- 「展開全部章節」改與普通章節共用按鈕樣式：最小高度 40、圓角 8、同一組邊框設定。先前更多按鈕最小高度 48、普通章節 40，造成大小和外觀不一致。
- 本機 analyze 通過：0 error／0 warning、13 條既有 info；[最新 CI 34303946612](https://github.com/funkeyyou/zaimanhua/actions/runs/34303946612) 的 78 個測試、Android APK、Windows ZIP 全數通過，測試包可由該次 CI 下載。
- MuMu 同一部作品比對：修正前簡介從三行（198 px）展開後反而變一行（66 px）；修正後為三行（198 px）→全文十行（660 px）→三行（198 px），按鈕依序顯示展開／收起／展開。
- MuMu 1080×1920／density 480：普通章節與更多按鈕均為 120 px 高，圓角、邊框一致。點更多後，第 68、67 話正常出現在原本箭頭的位置。
- 驗證僅操作簡介及章節列表，未開啟閱讀器或修改閱讀進度；截圖及 UI 結構位於忽略追蹤的 `build/qa-ui/detail-*.xml`、`description-*.xml`、`*-fixed.png`。

## 範圍

- 移除「我的訂閱」及「我的」內瀏覽紀錄入口；首頁訂閱預覽的更多入口改開書架。
- 書架第一列展示本機最近閱讀，隨可用寬度增加數量，章節／頁碼隨 Hive 儲存更新。
- 最近閱讀與訂閱互不排除。訂閱預設更新時間降序，其他排序選擇繼續保留。
- 依使用者回饋，全部進度／未讀更新／已追平改成篩選列中的單一下拉選單，節省一列空間。
- 本機瀏覽紀錄從書架右上進入，無網路可用；登入後提供雲端紀錄入口。
- 「我的」依用途分組；詳情頁以閱讀為主要操作，補齊圖示提示文字、章節進度、簡介展開與目前章節強調。
- 主頁以實際可用寬度配置，展開時全寬；有詳情且可用寬度至少 1000 時並排，保留兩側 Widget 與路由。
- 漫畫／小說輪播圖片保留 75:40 比例並限制為 280 邏輯高度；寬螢幕以相鄰推薦填滿空間，避免單張圖片占滿整個主頁。
- Android／Windows 使用新名稱與原創圖示。套件 ID、簽章、Hive 資料名稱、Windows 執行檔名稱及更新端點未更動。
- Windows 的 `ProductName` 保持 `zai_x`：path_provider 2.3.0 用這個欄位組成資料路徑，改名只改視窗標題、FileDescription 與 App 內顯示，避免舊資料看似消失。

## 驗證

- 新增版面測試：360 寬最近閱讀 3 本、1000 寬增加且維持一列、320 寬 1.8 倍文字無溢出。
- 新增資料回歸：保存新頁碼後最近閱讀即更新；閱讀過的作品仍在更新排序訂閱中。
- 新增導覽回歸：1200／400 寬切換、顯示／隱藏詳情均保留捲動位置與 Navigator 堆疊。
- 「我的」涵蓋小螢幕、寬螢幕、簡繁及 1.8 倍文字，並檢查閱讀統計入口仍可觸發。
- `flutter analyze --no-fatal-infos`：0 error、0 warning，13 條既有 info。
- 本機及首輪 CI 全部 77 個測試通過。初版 [34270950047](https://github.com/funkeyyou/zaimanhua/actions/runs/34270950047)、精簡進度選單版 [34272463572](https://github.com/funkeyyou/zaimanhua/actions/runs/34272463572) 的 analyze、Android、Windows 均成功。
- MuMu Android 15：1080×1920／density 480 的最近閱讀顯示 3 本；2208×1840／density 320 顯示 8 本。開啟詳情時並排、返回時書架恢復全寬，資料仍在。
- MuMu 實際操作本機紀錄、新版詳情、「我的」、簡繁切換與進度下拉選單。已追平能正確篩出作品，上方最近閱讀不受影響，操作後恢復全部進度。
- 「繼續閱讀」從既有第 10 頁恢復，退出閱讀器回到詳情，頁碼仍為第 10 頁。本次短暫試讀會按正常規則更新閱讀時間與末頁完成狀態。
- 最終本機 Android release APK：88,534,675 bytes，SHA-256 `1c29950ddb99b741852b8428e5fb64f063cfd71e6eb0d9b99adaa82cc45c6a50`，已覆蓋安裝 MuMu 驗證。
- MuMu 尺寸／密度已 reset、旋轉恢復 accelerometer_rotation=1／user_rotation=0、font_scale=1.0；介面語言還原跟隨系統。
- [34273571708](https://github.com/funkeyyou/zaimanhua/actions/runs/34273571708) 的 analyze、Android、Windows 均通過。Windows FileDescription 為「再漫畫X」、ProductName 為 `zai_x`；標題改用 C++ Unicode escape 後，啟動程序回報 MainWindowTitle 精確等於「再漫畫X」，Responding=true。
- Windows 控制工具最初因隱藏視窗回報 `launched app did not expose a targetable window`；正常顯示測試視窗後可操作，已確認「我的」、書架 10 本最近閱讀、訂閱載入與詳情並排。
- 補充輪播測試：360／1600 寬保留作品預覽可見空間，點擊仍能交給原本的作品開啟流程。
- 最終 [CI 34275340195](https://github.com/funkeyyou/zaimanhua/actions/runs/34275340195)：analyze、78 個測試、Android 及 Windows 建置全數通過。
- 最終輪播已在 MuMu 展開畫面與 Windows 實際視窗確認；Windows 書架／詳情頁／返回全寬操作通過。實體摺疊機仍由使用者驗收，未將模擬尺寸當成實機測試。

## 前一輪 UI 完整測試包（ba91095）

| 平台 | 位於 build/qa-ui/final/ | bytes | SHA-256 |
| --- | --- | ---: | --- |
| Android | android/ZAI-X-android.apk | 88,534,311 | `2181712bd4a5aceffaa88ebb1b1914a29444f1dab96163fdfe440f6122c27c61` |
| Windows | windows/ZAI-X-windows-x64.zip | 29,085,960 | `4cad942f3783e137696536bbb1beab96e177e94cddd4b37f122d6abac5f368f2` |

CI APK 簽章驗證通過，憑證 SHA-256 `4e2f84076e0c2a4361a8b23d9678b58b763bb43ecd45f437aa9bd0acd018b222`，與原正式版相同。兩平台測試包維持 1.9.0+19000；尚未建立新版 tag／Release。

測試截圖與二進位檔保存在忽略追蹤的 `build/qa-ui/`，不提交帳號畫面或使用者紀錄。

## 素材

圖示初稿採用原創向量繪製；後續內建生圖已恢復，試作比較後使用者確認保留 App 目前的向量圖示，v2.0.0 未套用生圖稿。見 [品牌素材](../assets/brand/README.md)。
